export type AIRow = {
  solution: string;
  type: string;
  keyFeatures: string[];
  bestFit: string[];
  notes?: string;
};

export const aiRows: AIRow[] = [
  // paste the same rows array you used on /ai here
];
